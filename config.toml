port = 3000
uploads_dir = "uploads"
# 超级管理员令牌，设置后可在 /admin 删除或上传镜像
# admin_token = "your-secret-token"

[[builds]]
name = "example"
interval_minutes = 60
script = """
#!/bin/bash
echo 'Build started at $(date)'
mkdir -p output
echo 'sample content' > output/sample.txt
"""
